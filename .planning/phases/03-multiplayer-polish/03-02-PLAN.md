---
phase: 03-multiplayer-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/game/MultiplayerLayout.tsx
  - src/components/game/MiniBoard.tsx
  - src/components/game/GarbageIndicator.tsx
  - src/styles/multiplayer.css
autonomous: true
requirements: [MULT-06, VISL-03]

must_haves:
  truths:
    - "Player sees their own board large and centered-left on screen"
    - "Player sees up to 3 opponent mini boards stacked vertically on the right"
    - "Mini boards show the opponent's grid, nickname, and lines cleared count"
    - "Layout adapts to player count: 1 mini for 2 players, 2 for 3, 3 for 4"
    - "Layout works on standard desktop browser widths (1024px+)"
  artifacts:
    - path: "src/components/game/MultiplayerLayout.tsx"
      provides: "Main multiplayer game layout container"
      min_lines: 60
    - path: "src/components/game/MiniBoard.tsx"
      provides: "Compact opponent board renderer"
      exports: ["MiniBoard"]
    - path: "src/components/game/GarbageIndicator.tsx"
      provides: "Visual garbage queue indicator on board side"
      exports: ["GarbageIndicator"]
    - path: "src/styles/multiplayer.css"
      provides: "Multiplayer layout styles and responsive breakpoints"
  key_links:
    - from: "src/components/game/MultiplayerLayout.tsx"
      to: "src/components/game/MiniBoard.tsx"
      via: "renders MiniBoard for each opponent"
      pattern: "<MiniBoard"
    - from: "src/components/game/MultiplayerLayout.tsx"
      to: "src/components/game/GarbageIndicator.tsx"
      via: "renders garbage indicator next to main board"
      pattern: "<GarbageIndicator"
---

<objective>
Build the multiplayer game screen layout with the player's main board and opponent mini boards, plus the garbage queue visual indicator.

Purpose: Players need to see their own game and their opponents' progress simultaneously during a match.
Output: MultiplayerLayout container, MiniBoard component, GarbageIndicator component, responsive styles.
</objective>

<execution_context>
@/Users/roviana/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roviana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multiplayer-polish/03-CONTEXT.md
@.planning/phases/02-game-engine/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build MultiplayerLayout and MiniBoard components</name>
  <files>src/components/game/MultiplayerLayout.tsx, src/components/game/MiniBoard.tsx, src/styles/multiplayer.css</files>
  <action>
Create MultiplayerLayout (src/components/game/MultiplayerLayout.tsx):
- Props: `players: PlayerState[]`, `currentPlayerId: string`, `pendingGarbage: number`, `children: ReactNode` (for the main game board)
- Layout structure per user decision: main board centered-left, opponent minis stacked vertically on the right sidebar
- Separate current player from opponents: `opponents = players.filter(p => p.playerId !== currentPlayerId)`
- Render children (main board) in the large left section (~70% width)
- Import and render `<GarbageIndicator pendingGarbage={pendingGarbage} />` alongside the main board (left side of the board area). The GarbageIndicator should sit immediately to the left of the children (main board), within the same flex/grid container so they appear as a unit.
- Render opponent MiniBoards in right sidebar (~30% width), stacked vertically
- Adapt to player count per user decision:
  - 2 players: 1 larger mini board (takes more vertical space)
  - 3 players: 2 mini boards
  - 4 players: 3 mini boards (compact)
- Use CSS grid or flexbox. Main board area minimum 600px wide.

Create MiniBoard (src/components/game/MiniBoard.tsx):
- Props: `player: PlayerState`, `size: "normal" | "large"` (large when only 1 opponent)
- Renders a scaled-down 10x20 board grid from `player.gameState.board`
- Each cell colored by piece type (same color scheme as main board but smaller)
- Below the grid: player nickname (truncated if long) + "Lines: {count}"
- When `player.eliminated === true`: gray overlay with "ELIMINATED" label and X icon per user decision
- When `player.connected === false`: same as eliminated (disconnected = instant elimination per user decision)
- Board cell size: ~8-12px for normal, ~14-18px for large variant

Create responsive styles (src/styles/multiplayer.css):
- Desktop-first (per VISL-03, desktop browsers)
- Min viewport: 1024px wide
- Main board area: flexible, centered-left
- Sidebar: fixed width ~280-320px, right side
- MiniBoards stack vertically with gap
- Eliminated state: grayscale filter + reduced opacity + "ELIMINATED" overlay
- Style for garbage indicator positioned to the left of the main board within the board area
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/game/MultiplayerLayout.tsx src/components/game/MiniBoard.tsx</automated>
  </verify>
  <done>MultiplayerLayout renders main board area with GarbageIndicator alongside, opponent sidebar with MiniBoards, eliminated boards display gray overlay, layout responds to 2/3/4 player counts</done>
</task>

<task type="auto">
  <name>Task 2: Build GarbageIndicator component</name>
  <files>src/components/game/GarbageIndicator.tsx</files>
  <action>
Create GarbageIndicator (src/components/game/GarbageIndicator.tsx):
- Props: `pendingGarbage: number`, `maxDisplay: number` (default 20, height of board)
- Renders a vertical bar on the left side of the main board (per user decision: garbage queue visible on side of board)
- Each pending garbage line = one filled segment in the bar, colored red/orange
- Bar grows from bottom up as garbage accumulates
- When garbage count exceeds maxDisplay, show a number badge at top
- Animate: new garbage segments slide in from bottom with brief pulse
- When garbage is cancelled (count decreases), segments fade out from top
- Use CSS transitions for smooth add/remove animations
- The bar should be narrow (~16-20px wide) and match the height of the main board
- Include a small label or tooltip showing exact pending count

Per user decision: "Garbage queue should be visible on the side of the board so players can see incoming threat and react strategically"
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/game/GarbageIndicator.tsx</automated>
  </verify>
  <done>GarbageIndicator renders vertical bar showing pending garbage count, animates additions and cancellations, positioned alongside main board</done>
</task>

</tasks>

<verification>
- All components compile with TypeScript
- MultiplayerLayout correctly separates current player from opponents
- MultiplayerLayout imports and renders GarbageIndicator next to the main board
- MiniBoard renders board grid at reduced scale with player info
- Eliminated state visually distinct (gray + label)
- GarbageIndicator shows pending garbage count with animations
- Layout works at 1024px+ viewport width
</verification>

<success_criteria>
- Player's main board is large and centered-left
- Up to 3 opponent mini boards visible on right sidebar
- Mini boards show grid, nickname, and lines cleared
- Layout adapts to 2/3/4 player counts
- Eliminated boards are grayed out with ELIMINATED label
- Garbage queue indicator visible on side of main board, wired into MultiplayerLayout
- Responsive for desktop browsers (1024px+)
</success_criteria>

<output>
After completion, create `.planning/phases/03-multiplayer-polish/03-02-SUMMARY.md`
</output>
