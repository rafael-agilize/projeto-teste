---
phase: 01-room-lobby
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/components/EntryScreen.tsx
  - src/lib/nickname.ts
autonomous: true
requirements:
  - ROOM-05

must_haves:
  truths:
    - "User can open the app and see a nickname input with Create Room and Join Room buttons"
    - "User can type a nickname (2-12 chars) and it validates inline"
    - "Nickname is persisted in localStorage and pre-filled on next visit"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "App entry point rendering EntryScreen"
    - path: "src/components/EntryScreen.tsx"
      provides: "Nickname input + Create/Join buttons"
      min_lines: 40
    - path: "src/lib/nickname.ts"
      provides: "Nickname validation and localStorage persistence"
      exports: ["validateNickname", "saveNickname", "loadNickname"]
  key_links:
    - from: "src/components/EntryScreen.tsx"
      to: "src/lib/nickname.ts"
      via: "import"
      pattern: "import.*nickname"
---

<objective>
Initialize the project and build the entry screen where players enter their nickname and choose to create or join a room.

Purpose: Establishes the project foundation and the first user-facing screen — the casual entry flow that is central to the game's low-friction design.
Output: Working Next.js app with entry screen at root URL.
</objective>

<execution_context>
@/Users/roviana/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roviana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-room-lobby/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with TypeScript and Tailwind</name>
  <files>package.json, tsconfig.json, next.config.ts, tailwind.config.ts, src/app/layout.tsx, src/app/globals.css</files>
  <action>
Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm` in the project root (it is currently empty except .planning and .git).

After scaffolding:
- Install socket.io and socket.io-client: `npm install socket.io socket.io-client`
- In layout.tsx: set page title to "Tetris Multiplayer", add a dark background body class (bg-gray-900 text-white)
- In globals.css: keep Tailwind directives, add a simple retro-style font import (e.g., Press Start 2P from Google Fonts or use system monospace as fallback)
- Clear out the default Next.js page content
- Add `.planning/` to .gitignore if not already there
  </action>
  <verify>
    <automated>cd /Users/roviana/Documents/Projetos/projeto-teste && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Next.js app builds successfully, dev server starts, dark themed empty page renders at localhost:3000</done>
</task>

<task type="auto">
  <name>Task 2: Build Entry Screen with nickname input and room actions</name>
  <files>src/components/EntryScreen.tsx, src/lib/nickname.ts, src/app/page.tsx</files>
  <action>
Create src/lib/nickname.ts:
- `validateNickname(name: string): { valid: boolean; error?: string }` — returns valid if 2-12 chars and not empty/whitespace-only. Letters, numbers, spaces allowed.
- `saveNickname(name: string): void` — saves to localStorage key "tetris-nickname"
- `loadNickname(): string` — reads from localStorage, returns empty string if not found

Create src/components/EntryScreen.tsx (client component — "use client"):
- State: nickname (string), joinCode (string), mode ("idle" | "joining"), error (string)
- On mount: load nickname from localStorage via loadNickname()
- Logo/title at top: "TETRIS BATTLE" in large retro-styled text (use font from Task 1 or bold monospace)
- Nickname input field with inline validation — show error below if invalid on blur/submit
- Two action buttons side by side:
  - "CREATE ROOM" — disabled if nickname invalid. On click: saves nickname, navigates to /room/create (we will handle this route in Plan 02)
  - "JOIN ROOM" — disabled if nickname invalid. On click: reveals a code input (4 uppercase letters), then a "JOIN" submit button. Navigates to /room/{code}
- Join code input: auto-uppercase, max 4 chars, only letters
- Style: centered card layout, dark theme, arcade-style buttons with hover effects, neon/bright accent colors (green, cyan, magenta)

Update src/app/page.tsx to render EntryScreen.

Per user decision: nickname persisted in localStorage, pre-filled on next visit, editable.
  </action>
  <verify>
    <automated>cd /Users/roviana/Documents/Projetos/projeto-teste && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Entry screen renders with nickname input (pre-filled from localStorage), Create Room and Join Room buttons, inline validation for 2-12 char nicknames, join code input that accepts 4 uppercase letters</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- Opening localhost:3000 shows the entry screen with dark theme
- Nickname validation works (rejects empty, 1-char, 13+ char names)
- Nickname persists across page refresh via localStorage
</verification>

<success_criteria>
- Next.js project initialized with TypeScript, Tailwind, Socket.io deps
- Entry screen is the app root with nickname input + Create/Join buttons
- Nickname validation (2-12 chars) works with inline feedback
- Nickname saved to and loaded from localStorage
- Dark retro-themed visual design
</success_criteria>

<output>
After completion, create `.planning/phases/01-room-lobby/01-01-SUMMARY.md`
</output>
